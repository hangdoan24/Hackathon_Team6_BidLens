# One key is enough: it powers BOTH the analysis pipeline and the chat assistant.
GEMINI_API_KEY=

# Optional. Only fill this in if you want the chat assistant to run on Claude
# instead of Gemini. Leave empty to use the key above for everything.
ANTHROPIC_API_KEY=
